services:
  haproxy:
    image: haproxy:alpine
    restart: unless-stopped
    container_name: stalwart-proxy
    network_mode: host
    cap_add:
      - NET_BIND_SERVICE
    volumes:
      - ./haproxy.cfg:/usr/local/etc/haproxy/haproxy.cfg:ro
      - ./hosts:/etc/hosts:ro
    user: root

  stalwart:
    image: stalwartlabs/stalwart:latest
    restart: unless-stopped
    container_name: stalwart-mail
    networks:
      - internal   # NUR internes Netzwerk – keine Host-Ports mehr!
    volumes:
      - stalwart-etc:/etc/stalwart
      - stalwart-data:/var/lib/stalwart
      - stalwart-log:/var/log/stalwart

volumes:
  stalwart-etc:
    external: true
  stalwart-data:
    external: true
  stalwart-log:
    external: true

networks:
  internal:
    driver: bridge
    ipam:
      config:
        - subnet: 10.42.37.0/24        # Ihr gewünschter fester IP-Bereich
          gateway: 10.42.37.1          # Gateway/Nameserver für dieses Netzwerk
