services:
  haproxy:
    image: haproxy:alpine
    restart: unless-stopped
    container_name: stalwart-proxy
    network_mode: host
    cap_add:
      - NET_BIND_SERVICE
    volumes:
      - ./etc/haproxy/haproxy.cfg:/usr/local/etc/haproxy/haproxy.cfg:ro
      - ./etc/hosts:/etc/hosts:ro
    user: root

  stalwart:
    image: stalwartlabs/stalwart:latest
    restart: unless-stopped
    container_name: stalwart-mail
    networks:
      mailnet:   # Only internel Network – no hosts ports!
        ipv4_address: 10.42.37.2           # Fixed IP
    volumes:
      - stalwart-etc:/etc/stalwart
      - stalwart-data:/var/lib/stalwart
    # No ports - Stalwart is only reachable internally

volumes:
  stalwart-etc:
    external: true
  stalwart-data:
    external: true

networks:
  mailnet:
    driver: bridge
    ipam:
      config:
        - subnet: 10.42.37.0/24        # The fixed sub-network
          gateway: 10.42.37.1          # Gateway for that network
